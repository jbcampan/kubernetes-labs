# Zero Trust baseline: block ALL ingress and egress for every pod
# in this namespace. Nothing is allowed until explicitly permitted below.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-all
  namespace: netpol-demo
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress