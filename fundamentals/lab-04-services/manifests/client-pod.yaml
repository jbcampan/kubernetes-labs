apiVersion: v1
kind: Pod
metadata:
  name: dns-client
  labels:
    role: network-tester
spec:
  containers:
    - name: dns-client
      image: busybox:1.36
      command: ["sleep", "3600"]
  restartPolicy: Never