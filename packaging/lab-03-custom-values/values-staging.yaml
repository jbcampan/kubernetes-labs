# Staging: closer to prod behaviour, resource-constrained,
# more than one replica to catch scheduling/selector issues early.
replicaCount: 2

image:
  tag: "1.27-alpine"

resources:
  requests:
    cpu: "100m"
    memory: "64Mi"
  limits:
    cpu: "250m"
    memory: "128Mi"