# A dedicated technical identity, distinct from the admin user used so far
# via kubeconfig. This represents, for example, a CI/CD pipeline that only
# needs to inspect Pods and Deployments in one namespace — never create,
# update or delete anything.

apiVersion: v1
kind: ServiceAccount
metadata:
  name: ci-deployer
  namespace: rbac-demo