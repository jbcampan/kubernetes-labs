apiVersion: v1
kind: Pod
metadata:
  name: crashing-app
  labels:
    app: debug-lab
    scenario: crash-loop-backoff
spec:
  containers:
    - name: app
      image: busybox:1.36
      command: ["sh", "-c", "echo 'Simulating a fatal startup error' && exit 1"]
  restartPolicy: Always