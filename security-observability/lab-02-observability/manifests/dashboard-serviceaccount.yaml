# ServiceAccount dedicated to accessing the Kubernetes Dashboard.
# Deliberately NOT named "admin-user" and NOT bound to cluster-admin:
# this lab practices least-privilege, read-only access (see dashboard-rbac.yaml).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: dashboard-viewer
  namespace: kubernetes-dashboard