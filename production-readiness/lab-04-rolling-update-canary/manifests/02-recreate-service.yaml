apiVersion: v1
kind: Service
metadata:
  name: recreate-demo-svc
  labels:
    app: recreate-demo
spec:
  type: ClusterIP
  selector:
    app: recreate-demo
  ports:
    - port: 80
      targetPort: 5678