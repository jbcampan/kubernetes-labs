apiVersion: v1
kind: Service
metadata:
  name: rolling-demo-svc
  labels:
    app: rolling-demo
spec:
  type: ClusterIP
  selector:
    app: rolling-demo
  ports:
    - port: 80
      targetPort: 5678